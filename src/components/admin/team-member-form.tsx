import type { TeamMember } from "@/lib/types";

export function TeamMemberForm({ member, action }: { member?: TeamMember; action: (formData: FormData) => void | Promise<void> }) {
  return <form action={action} className="admin-form"><div className="admin-form-grid"><label>Nom *<input name="name" required defaultValue={member?.name} placeholder="Chakib A." /></label><label>Slug *<input name="slug" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" defaultValue={member?.slug} placeholder="chakib-a" /></label></div><div className="admin-form-grid"><label>Initiales *<input name="initials" required minLength={2} maxLength={4} defaultValue={member?.initials} placeholder="CA" /></label><label>Rôle *<input name="role" required defaultValue={member?.role} placeholder="Co-fondateur · Développeur web" /></label></div><label>Bio *<textarea name="bio" required minLength={20} rows={4} defaultValue={member?.bio || ""} /></label><label>Compétences <small>séparées par des virgules</small><input name="skills" defaultValue={member?.skills.join(", ")} placeholder="Next.js, UI, Performance" /></label><div className="admin-form-grid"><label>E-mail de contact<input name="contact" type="email" defaultValue={member?.contact || ""} placeholder="kicoagency@siteprofree.email" /></label><label>Localisation<input name="location" defaultValue={member?.location || ""} placeholder="Algérie" /></label></div><div className="admin-form-grid"><label>Disponibilité<input name="availability" defaultValue={member?.availability || ""} placeholder="Disponible pour nouveaux projets" /></label><label>Ordre d’affichage<input name="sortOrder" type="number" min={0} defaultValue={member?.sortOrder ?? 0} /></label></div><label>Mise en avant du profil<textarea name="highlight" rows={2} maxLength={300} defaultValue={member?.highlight || ""} placeholder="Une phrase courte qui résume le profil." /></label><div className="admin-checks"><label><input name="published" type="checkbox" defaultChecked={member?.published ?? true} /> Publié</label></div><button type="submit">{member ? "Enregistrer le profil" : "Créer le profil"}</button></form>;
}
